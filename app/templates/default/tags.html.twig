{% extends 'template.html.twig' %}

{% block title %}Tags{% endblock %}

{% block content %}
    <ul class="tags">
    {% for tag in tags %}
        <li><a href="/tags/{{ tag.getSlug }}">{{ tag.getName }} ({{ tag.getPosts | length }})</a></li>
    {% endfor %}
    </ul>
{% endblock %}
